{
  "name": "task-recorder-extension",
  "version": "1.0.0",
  "description": "Chrome extension for recording browser tasks",
  "type": "module",
  "scripts": {
    "build": "node build.js && cp manifest.json dist/ && cp popup.html dist/ && cp styles.css dist/",
    "watch": "node build.js --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}

